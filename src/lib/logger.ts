/**
 * @deprecated Use `@/lib/services/logger.service` instead.
 *
 * This file is kept as a re-export shim to avoid breaking any remaining
 * imports that still reference the old path. Remove it once all
 * consumers have been updated.
 */
export { logger } from "./services/logger.service";
